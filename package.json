{
  "name": "stello",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/core/tsconfig.json",
    "test": "node --import tsx --test packages/core/src/*.test.ts",
    "relayer": "node --import tsx scripts/relayer.ts",
    "e2e": "node --import tsx scripts/e2e.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
